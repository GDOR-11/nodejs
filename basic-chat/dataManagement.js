/**
 * map from socket IDs to the respective usernames
 * @type {Object}
 */
export const usernames = {};

/**
 * it's the chat history, doesn't get any simpler than that
 * @type {Array.<import("./public/message.js").Message>}
 */
export const chatHistory = [];